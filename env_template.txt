# LLM API 配置模板
# 使用方法：将此文件重命名为 .env 并填写你的实际配置

# API 基础地址（必须包含 /v1）
BASE_URL=https://api.deepseek.com/v1

# API 密钥（替换为你的实际密钥）
API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 模型名称
MODEL=deepseek-chat

# 温度参数（0-1，控制输出随机性）
TEMPERATURE=0

# XGBoost 混合权重（0-1，默认 0.5）
# XGB_BLEND_ALPHA=0.5

